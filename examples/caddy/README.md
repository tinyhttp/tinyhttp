# Multi-process (cluster) example

Simple example of a tinyhttp app with Caddy

## Prerequisites

- `caddy`: v2.2.1+

## Setup

```sh
pnpm install
```

## Run

Run caddy:

```sh
caddy run
```

Run the app:

```sh
node index.js
```
